const client = {
    port: 0000,
    host: "0.0.0.0"
};

const server = {
    port: 0000,
    host: "0.0.0.0"
};

const mongo = {
    port: 0000,
    host: "0.0.0.0"
};

module.exports.client = client;
module.exports.server = server;
module.exports.mongo = mongo;